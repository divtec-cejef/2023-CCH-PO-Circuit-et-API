{
	"name": "api",
	"version": "1.0.0",
	"description": "",
	"main": "index.ts",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "npm i && prisma db pull && prisma generate && npx ts-node ."
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"@prisma/client": "^4.12.0",
		"@types/express": "^4.17.17",
		"express": "^4.18.2",
		"fs": "^0.0.1-security",
		"typescript": "^5.0.2"
	},
	"devDependencies": {
		"prisma": "^4.12.0"
	}
}
